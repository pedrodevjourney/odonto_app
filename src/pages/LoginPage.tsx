import { LoginForm } from '@/components/LoginForm';

export function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[#A8DFF5]/20 p-4">
      <LoginForm />
    </main>
  );
}
