# Copie para .env.local (desenvolvimento) ou .env.production (build publicado).
VITE_API_BASE_URL=http://localhost:8080

# Client ID OAuth do Google, usado na integração com o Google Calendar.
# Em produção, cadastre o domínio publicado nas "Authorized JavaScript origins".
VITE_GOOGLE_CLIENT_ID=
