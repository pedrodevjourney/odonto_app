# Variáveis do build de produção (Vite lê em build time, não em runtime).
# TROQUE pelos valores reais antes de publicar; no Cloudflare Pages você também
# pode definir as mesmas chaves em Settings > Environment variables.
VITE_API_BASE_URL=https://SUA-API.onrender.com
VITE_GOOGLE_CLIENT_ID=
